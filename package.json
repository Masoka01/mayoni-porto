{
  "name": "mayoni-portfolio",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i tailwind/input.css -o css/tailwind.css --minify",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
